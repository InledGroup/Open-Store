---
import Layout from '../../layouts/Layout.astro';

const baseUrl = Astro.url.origin;
---

<Layout title="API Documentation - App Store">
  <div class="api-docs">
    <div class="container">
      <div class="docs-header">
        <h1>📡 API Documentation</h1>
        <p class="subtitle">Accede a toda la información de aplicaciones mediante nuestra API REST</p>
      </div>

      <div class="docs-grid">
        <!-- Introducción -->
        <section class="docs-section">
          <h2>🚀 Introducción</h2>
          <p>La Inled Open Store API proporciona acceso completo a todos los datos de aplicaciones en formato JSON.</p>

          <div class="info-box">
            <strong>Base URL:</strong> <code>{baseUrl}/api</code>
          </div>

          <div class="feature-grid">
            <div class="feature-item">
              <span class="feature-icon">⚡</span>
              <h3>Rápida</h3>
              <p>Respuestas optimizadas con cache</p>
            </div>
            <div class="feature-item">
              <span class="feature-icon">🔓</span>
              <h3>Abierta</h3>
              <p>Sin autenticación requerida</p>
            </div>
            <div class="feature-item">
              <span class="feature-icon">📦</span>
              <h3>RESTful</h3>
              <p>Arquitectura REST estándar</p>
            </div>
          </div>
        </section>

        <!-- Endpoints -->
        <section class="docs-section">
          <h2>🔌 Endpoints</h2>

          <div class="endpoint">
            <div class="endpoint-header">
              <span class="method get">GET</span>
              <code class="path">/api/apps.json</code>
            </div>
            <p>Obtiene la lista completa de aplicaciones.</p>
            <div class="example">
              <strong>Ejemplo:</strong>
              <pre><code>curl {baseUrl}/api/apps.json</code></pre>
            </div>
            <a href="/api/apps.json" target="_blank" class="btn btn-primary btn-sm">Ver ejemplo en vivo →</a>
          </div>

          <div class="endpoint">
            <div class="endpoint-header">
              <span class="method get">GET</span>
              <code class="path">/api/apps/[id].json</code>
            </div>
            <p>Obtiene los detalles de una aplicación específica.</p>
            <div class="params">
              <strong>Parámetros:</strong>
              <ul>
                <li><code>id</code> - ID único de la aplicación</li>
              </ul>
            </div>
            <div class="example">
              <strong>Ejemplo:</strong>
              <pre><code>curl {baseUrl}/api/apps/swiftinstall.json</code></pre>
            </div>
            <a href="/api/apps/swiftinstall.json" target="_blank" class="btn btn-primary btn-sm">Ver ejemplo en vivo →</a>
          </div>

          <div class="endpoint">
            <div class="endpoint-header">
              <span class="method get">GET</span>
              <code class="path">/api/stats.json</code>
            </div>
            <p>Obtiene estadísticas generales de la app store.</p>
            <div class="example">
              <strong>Ejemplo:</strong>
              <pre><code>curl {baseUrl}/api/stats.json</code></pre>
            </div>
            <a href="/api/stats.json" target="_blank" class="btn btn-primary btn-sm">Ver ejemplo en vivo →</a>
          </div>

          <div class="endpoint">
            <div class="endpoint-header">
              <span class="method get">GET</span>
              <code class="path">/api/platforms/[platform].json</code>
            </div>
            <p>Filtra aplicaciones por plataforma.</p>
            <div class="params">
              <strong>Plataformas válidas:</strong>
              <div class="platform-badges">
                <span class="badge">Windows</span>
                <span class="badge">macOS</span>
                <span class="badge">Linux</span>
                <span class="badge">Web</span>
                <span class="badge">Android</span>
                <span class="badge">iOS</span>
                <span class="badge">Chrome</span>
                <span class="badge">Firefox</span>
                <span class="badge">Safari</span>
                <span class="badge">Edge</span>
              </div>
            </div>
            <div class="example">
              <strong>Ejemplo:</strong>
              <pre><code>curl {baseUrl}/api/platforms/Windows.json</code></pre>
            </div>
            <a href="/api/platforms/Windows.json" target="_blank" class="btn btn-primary btn-sm">Ver ejemplo en vivo →</a>
          </div>
        </section>

        <!-- Modelo de datos -->
        <section class="docs-section">
          <h2>📋 Modelo de Datos</h2>

          <div class="code-block">
            <pre><code>{`{
  "id": "string",
  "title": "string",
  "icon": "string",
  "platform": ["Platform"],
  "shortDescription": "string",
  "description": "string (markdown)",
  "screenshots": ["string"],
  "video": "string | undefined",
  "downloads": {
    "github": "string | undefined",
    "sourceforge": "string | undefined",
    "sourceCode": "string"
  }
}`}</code></pre>
          </div>
        </section>

        <!-- Ejemplos de uso -->
        <section class="docs-section">
          <h2>💻 Ejemplos de Uso</h2>

          <div class="example-tabs">
            <h3>JavaScript / Fetch</h3>
            <div class="code-block">
              <pre><code>{`// Obtener todas las apps
fetch('${baseUrl}/api/apps.json')
  .then(res => res.json())
  .then(apps => console.log(apps));

// Obtener app específica
fetch('${baseUrl}/api/apps/photo-editor-pro.json')
  .then(res => res.json())
  .then(app => console.log(app));

// Filtrar por plataforma
fetch('${baseUrl}/api/platforms/Windows.json')
  .then(res => res.json())
  .then(data => console.log(data.apps));`}</code></pre>
            </div>

            <h3>Python</h3>
            <div class="code-block">
              <pre><code>{`import requests

# Obtener todas las apps
response = requests.get('${baseUrl}/api/apps.json')
apps = response.json()

# Obtener estadísticas
stats = requests.get('${baseUrl}/api/stats.json').json()
print(f"Total apps: {stats['totalApps']}")`}</code></pre>
            </div>
          </div>
        </section>

        <!-- Rate Limiting & Cache -->
        <section class="docs-section">
          <h2>⚙️ Configuración</h2>

          <div class="config-grid">
            <div class="config-item">
              <h3>Cache</h3>
              <p>Los endpoints están cacheados durante 1 hora (3600 segundos)</p>
              <code>Cache-Control: public, max-age=3600</code>
            </div>

            <div class="config-item">
              <h3>CORS</h3>
              <p>CORS está habilitado para todas las peticiones</p>
              <code>Access-Control-Allow-Origin: *</code>
            </div>

            <div class="config-item">
              <h3>Formato</h3>
              <p>Todas las respuestas están en formato JSON</p>
              <code>Content-Type: application/json</code>
            </div>
          </div>
        </section>
      </div>
    </div>
  </div>
</Layout>

<style>
  .api-docs {
    padding: 40px 0 80px;
    background: var(--bg-primary);
    min-height: calc(100vh - 140px);
  }

  .docs-header {
    text-align: center;
    margin-bottom: 60px;
    padding-top: 20px;
  }

  .docs-header h1 {
    font-size: 3rem;
    font-weight: 800;
    margin-bottom: 16px;
    background: linear-gradient(135deg, #3db6ff 0%, #2a9de6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    font-size: 1.25rem;
    color: var(--text-secondary);
  }

  .docs-grid {
    display: flex;
    flex-direction: column;
    gap: 40px;
    max-width: 900px;
    margin: 0 auto;
  }

  .docs-section {
    background: var(--bg-secondary);
    border-radius: var(--radius-md);
    padding: 32px;
    border: 1px solid var(--border);
    box-shadow: var(--shadow-sm);
  }

  .docs-section h2 {
    font-size: 1.75rem;
    margin-bottom: 20px;
    color: var(--text-primary);
  }

  .docs-section h3 {
    font-size: 1.25rem;
    margin-bottom: 12px;
    margin-top: 24px;
    color: var(--text-primary);
  }

  .docs-section p {
    color: var(--text-secondary);
    line-height: 1.8;
    margin-bottom: 16px;
  }

  .info-box {
    background: var(--bg-primary);
    padding: 16px;
    border-radius: var(--radius-sm);
    margin: 20px 0;
    border-left: 4px solid var(--primary);
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 20px;
    margin-top: 24px;
  }

  .feature-item {
    text-align: center;
    padding: 20px;
    background: var(--bg-primary);
    border-radius: var(--radius-sm);
    transition: var(--transition);
  }

  .feature-item:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
  }

  .feature-icon {
    font-size: 2rem;
    display: block;
    margin-bottom: 12px;
  }

  .feature-item h3 {
    margin: 0 0 8px 0;
    font-size: 1.1rem;
  }

  .feature-item p {
    margin: 0;
    font-size: 0.9rem;
  }

  .endpoint {
    background: var(--bg-primary);
    padding: 20px;
    border-radius: var(--radius-sm);
    margin-bottom: 20px;
    border: 1px solid var(--border);
  }

  .endpoint:last-child {
    margin-bottom: 0;
  }

  .endpoint-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 12px;
  }

  .method {
    padding: 4px 12px;
    border-radius: 4px;
    font-weight: 700;
    font-size: 0.85rem;
    text-transform: uppercase;
  }

  .method.get {
    background: #10b981;
    color: white;
  }

  .path {
    background: var(--bg-secondary);
    padding: 6px 12px;
    border-radius: 6px;
    font-family: 'Courier New', monospace;
    font-size: 0.9rem;
    color: var(--primary);
    border: 1px solid var(--border);
  }

  .params {
    margin: 16px 0;
  }

  .params ul {
    list-style: none;
    padding: 0;
  }

  .params li {
    padding: 8px 0;
    color: var(--text-secondary);
  }

  .example {
    margin: 16px 0;
  }

  .example pre {
    background: var(--bg-secondary);
    padding: 16px;
    border-radius: var(--radius-sm);
    overflow-x: auto;
    border: 1px solid var(--border);
    margin-top: 8px;
  }

  .example code {
    font-family: 'Courier New', monospace;
    font-size: 0.9rem;
    color: var(--text-primary);
  }

  .code-block {
    margin: 20px 0;
  }

  .code-block pre {
    background: var(--bg-primary);
    padding: 20px;
    border-radius: var(--radius-sm);
    overflow-x: auto;
    border: 1px solid var(--border);
  }

  .code-block code {
    font-family: 'Courier New', monospace;
    font-size: 0.9rem;
    color: var(--text-primary);
    line-height: 1.6;
  }

  .btn-sm {
    padding: 8px 16px;
    font-size: 0.875rem;
    margin-top: 12px;
  }

  .platform-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 12px;
  }

  .badge {
    display: inline-block;
    padding: 6px 12px;
    background: var(--primary);
    color: white;
    border-radius: 6px;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .example-tabs {
    margin-top: 20px;
  }

  .config-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
    margin-top: 20px;
  }

  .config-item {
    background: var(--bg-primary);
    padding: 20px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--border);
  }

  .config-item h3 {
    margin-top: 0;
    font-size: 1.1rem;
  }

  .config-item p {
    margin-bottom: 12px;
  }

  .config-item code {
    display: block;
    background: var(--bg-secondary);
    padding: 8px 12px;
    border-radius: 4px;
    font-size: 0.85rem;
    color: var(--primary);
    border: 1px solid var(--border);
  }

  @media (max-width: 768px) {
    .docs-header h1 {
      font-size: 2rem;
    }

    .subtitle {
      font-size: 1rem;
    }

    .docs-section {
      padding: 24px 20px;
    }

    .endpoint-header {
      flex-direction: column;
      align-items: flex-start;
    }

    .feature-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
